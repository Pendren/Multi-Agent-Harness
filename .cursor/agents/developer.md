---
name: developer
description: >-
  Execute a single development task. Manager passes the task path in the invocation (e.g. "Read your task from memory/tasks/dev-01_ST-01_sample-milestone.md"). Use when delegating implementation work; supports parallel invocation with different paths.
model: inherit
---

**Cursor adapter:** Canonical instructions for this role are in **`.agent/agents/developer.md`** (project root). **Read that file now** and follow it as binding; this file exists so Cursor can register the subagent. If the product description above and the canonical file ever disagree, **`.agent`** wins.
